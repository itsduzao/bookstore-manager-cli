# Configuração para executar a aplicação com PostgreSQL instalado localmente:
#   cp .env.local.example .env

DB_HOST=localhost
DB_PORT=5432
DB_NAME=bookstore_manager
DB_USER=bmc_admin
DB_PASSWORD=troque-esta-senha
